using System.Collections;
using System.Collections.Generic;
using Microsoft.MixedReality.Toolkit.XRSDK.OpenXR;
using UnityEngine;

namespace Windows.Media.MixedRealityCapture
{




    /// <summary>
    /// this script is meant to handle the video captured by the hololens
    /// and help to make it so that the real world and the holographic world
    /// line up as they are supposed to.
    /// </summary>
    public class HolographicMRCManager : MonoBehaviour

    {
        // Start is called before the first frame update
        void Start()
        {
            
        }

        // Update is called once per frame

        void Update()
        {

        }
    }
}